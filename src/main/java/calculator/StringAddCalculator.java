package calculator;

public class StringAddCalculator {
    public static int add(String text) {
        if (text == null || text.isEmpty()) {
            return 0;
        }
        return 0; // 기본 반환값
    }
}
