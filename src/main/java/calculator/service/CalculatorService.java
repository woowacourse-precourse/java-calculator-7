package calculator.service;

public class CalculatorService {

    public int calculate(String input) {
        int answer = 0;
        if (input.isEmpty()) {
            return answer;
        }

        return answer;
    }
}
