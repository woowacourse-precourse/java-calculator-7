package calculator.cotroller;

import calculator.model.UserInputData;
import calculator.service.CalculateService;
import calculator.service.NumValidService;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import java.util.List;

import static org.assertj.core.api.Assertions.assertThat;
import static org.junit.jupiter.api.Assertions.*;
class UserApiControllerTest {
    /*
    UserApiController userApiController = new UserApiController();
    UserInputData userInputData = new UserInputData();
    NumValidService numValidService = new NumValidService();
    CalculateService calculateService = new CalculateService();

    @DisplayName("UserApiController - saveData()")
    @Test
    void saveDataTest(){
        //given
        String str = "//!\n1!1!1:1,";
        //when
        userInputData.setInput(str);
        numValidService.inputValid(userInputData.getInput());
        calculateService.extractCustoms(userInputData.getInput());
        int res = calculateService.calculate(userApiController.getCustoms(), userInputData.getInput());
        UserRequestController.requestView(res);

        //then
        assertThat(userInputData.getInput()).isEqualTo(str);
        assertThat(userApiController.getCustoms().size()).isNotZero();
        assertThat(res).isEqualTo(4);

    }

*/
}
/*
    @DisplayName("")
    @Test
   */